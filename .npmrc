# Flat node_modules (npm-like) so Prisma's custom client output
# (packages/dashboard-api/node_modules/.prisma/client) resolves at runtime.
# Matches the production Docker build, which sets the same. Without this, pnpm's
# default isolated linker leaves @prisma/client pointing at a stale store copy.
node-linker=hoisted
