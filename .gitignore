# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
builds/

# WhatsApp session data — NEVER commit (contains account credentials)
/sessions/
packages/wa-server/sessions/

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/

# ─── INTERNAL DOCS — NEVER COMMIT TO PUBLIC REPO (NO EXCEPTIONS) ───
# Design docs, PRDs, strategy, roadmaps, competitive analysis stay LOCAL only.
# See CLAUDE.md "Public Repo Hygiene". Public-facing docs go in README/CHANGELOG.
.claude/
docs/
/docs/*
docs/designs/
docs/internal/
*.PRD.md
CLAUDE.md
docker-compose.test-ports.yml

# Internal audit / strategy notes — never push to the public repo
*_AUDIT*.md
